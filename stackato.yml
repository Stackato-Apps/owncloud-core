name: owncloud
instances: 1
framework:
  type: php
mem: 128
services:
  ${name}-db: mysql
  ${name}-fs: filesystem
hooks:
  post-staging:
    - mkdir $STACKATO_FILESYSTEM/data
  pre-running:
    - ln -s $STACKATO_FILESYSTEM/data data
